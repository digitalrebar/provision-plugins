---
Name: "image-deploy/image-url"
Description: "The image url for image-deploy to use."
Documentation: |
  The URL location for the image for image-deploy is to install.  This must be a
  URL that is accessible from the Machine being deployed at the time of deployment.

Schema:
  type: string
Meta:
  icon: "image"
  color: "blue"
  title: "RackN Content"
