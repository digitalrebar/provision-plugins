---
Name: "image-deploy/image-os"
Description: "The OS type of the install image."
Documentation: |
  The file image contains the install contents for an
  operating system.  This variable defines the image
  OS at a high level.

  Valid values are:

    * esxi
    * linux
    * windows

  .. note: The type ``basic`` has been deprecated and is no longer supported.

Schema:
  type: "string"
  enum:
    - "basic"
    - "esxi"
    - "linux"
    - "windows"
Meta:
  icon: "image"
  color: "blue"
  title: "RackN Content"
