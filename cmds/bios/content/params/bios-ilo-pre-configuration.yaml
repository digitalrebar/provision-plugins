---
Name: bios-ilo-pre-configuration
Description: "The ILO configuration xml files to apply to the system before bios-configure"
Documentation: |
  This is a list of xml strings to apply to the system through hponcfg.

  These are applied before bios-configure.
Meta:
  color: grey
  icon: money
Schema:
  type: array
  default: []
  items:
    type: string
