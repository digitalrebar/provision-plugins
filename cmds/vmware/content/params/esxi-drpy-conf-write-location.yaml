---
Name: esxi/drpy-agent-conf
Description: "Defines where the DRPY Agent configuration file will be rendered to."
Documentation: |
  The location that the "DRPY" agent configuration file  will be
  rendered to the on the local ESXI system.

  Setting this value will completely override the in-built default
  location which is:

    * ``_esxi/install-datastore_/rackn/drpy.conf``

  This is not defined with a default value as we use the above Param
  (_esxi/install-datastore_) as a base for the path.

Meta:
  color: blue
  icon: hashtag
  title: RackN Content
  password: showable
ReadOnly: true
Schema:
  type: string
Secure: false
